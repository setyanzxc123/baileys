export const OTP_PATTERN = /^\d{4,8}$/;

export const DEFAULT_APP_NAME = 'WhatsApp Gateway';
export const DEFAULT_DOC_NAME = 'document.pdf';
export const DEFAULT_DOC_MIMETYPE = 'application/pdf';

export const DISCONNECT_REASON = {
  LOGGED_OUT: 401,
  RESTART_REQUIRED: 515,
};
